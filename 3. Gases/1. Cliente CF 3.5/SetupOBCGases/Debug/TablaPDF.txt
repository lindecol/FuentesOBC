CREATE TABLE DTSC_LOG_DESCARGA_PDF (
  ID NUMBER GENERATED ALWAYS AS IDENTITY,
  Terminal  VARCHAR2(50)  NOT NULL,
  Archivo  VARCHAR2(500)  NOT NULL,
  Fecha DATE DEFAULT (sysdate));

ALTER TABLE DTSC_LOG_DESCARGA_PDF ADD (
  CONSTRAINT DTSC_LOG_DESCARGA_PDF_pk PRIMARY KEY (ID));



